# Пример конфига nginx — скопируй в /etc/nginx/sites-available/math-base
# и сделай symlink в sites-enabled

server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Перенаправление на HTTPS (раскомментируй после настройки certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://localhost:3042;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS (после certbot --nginx)
# server {
#     listen 443 ssl;
#     server_name yourdomain.com www.yourdomain.com;
#     ...
# }
